(function(){const $=id=>document.getElementById(id);const on=(sel,ev,fn)=>{const el=$(sel);if(el)el.addEventListener(ev,fn);};const nowISO=()=>new Date().toISOString();const uid=()=>(crypto&&crypto.randomUUID)?crypto.randomUUID():('id-'+Date.now()+'-'+Math.floor(Math.random()*10000));function formatCurrency(value,currency='COP'){return Number(value||0).toLocaleString('es-CO',{style:'currency',currency,minimumFractionDigits:2,maximumFractionDigits:2});}function parseCurrencyFormatted(formattedValue){const cleanValue=String(formattedValue).replace(/[^\d,]/g,'').replace(/\./g,'').replace(',','.');const parsed=parseFloat(cleanValue||0);return isNaN(parsed)?0:parsed;}function createCurrencyMask(inputElement){inputElement.addEventListener("input",(e)=>{let value=e.target.value;value=value.replace(/\D/g,"");if(value===""){e.target.value="";return;}if(value.length===1){value="0"+value;}const cents=value.slice(-2);const integer=value.slice(0,-2);const formattedInt=integer===""?"0":parseInt(integer).toLocaleString("es-CO");e.target.value=`${formattedInt},${cents}`;});inputElement.addEventListener("blur",(e)=>{const value=parseCurrencyFormatted(e.target.value);e.target.value=value.toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2});});inputElement.addEventListener("focus",(e)=>{const value=parseCurrencyFormatted(e.target.value);e.target.value=value===0?"":value.toString();});}function initializeCurrencyMasks(){document.querySelectorAll('.currency-input').forEach(input=>{createCurrencyMask(input);});}function showToast(message,type='info',duration=5000){const container=$('toast-container');if(!container)return;const toast=document.createElement('div');toast.className=`toast ${type}`;toast.innerHTML=`<div class="toast-icon">${type==='success'?'✓':type==='error'?'✕':type==='warning'?'⚠':'ℹ'}</div><div class="toast-message">${message}</div>`;container.appendChild(toast);setTimeout(()=>{if(!toast.parentNode)return;toast.style.animation='toastOut 0.3s ease forwards';setTimeout(()=>{if(toast.parentNode)container.removeChild(toast);},300);},duration);}const STORAGE_KEY='banklar_finances_v8';function saveState(s){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(s));return true;}catch(e){showToast('Error al guardar datos','error');console.error(e);return false;}}function loadState(){try{const raw=localStorage.getItem(STORAGE_KEY);return raw?JSON.parse(raw):null;}catch(e){console.error('Error loading state',e);return null;}}function copyDataToClipboard(){const data=JSON.stringify(state,null,2);const textarea=$('data-export-text');if(textarea){textarea.value=data;textarea.select();document.execCommand('copy');showToast('Datos copiados al portapapeles','success');}}function importDataFromClipboard(){const textarea=$('data-import-text');if(!textarea||!textarea.value.trim()){showToast('No hay datos para importar','error');return;}try{const imported=JSON.parse(textarea.value);if(!imported.user||!imported.transactions){showToast('Datos inválidos','error');return;}if(!confirm('⚠️ ¿Estás seguro de importar estos datos? Se perderán todos los datos actuales.')){return;}state=imported;if(!state.transactions[0]?.timestamp){state.transactions.forEach(tx=>{if(tx.date&&!tx.timestamp){tx.timestamp=new Date(tx.date).getTime();tx.hour=new Date(tx.date).getHours();tx.minute=new Date(tx.date).getMinutes();}});}if(!state.user.bancolombia&&state.user.bancolombia!==0){state.user.bancolombia=0;}if(saveState(state)){showToast('Datos importados correctamente','success');hideAllModals();renderAll();location.reload();}}catch(e){console.error('Error importing data:',e);showToast('Error al importar datos: formato inválido','error');}}function showPortabilityModal(){showOverlay();const modal=$('portability-modal');if(!modal)return;const exportData={...state,meta:{...state.meta,exportedAt:nowISO(),version:'v8'}};const textarea=$('data-export-text');if(textarea){textarea.value=JSON.stringify(exportData,null,2);}modal.classList.remove('hidden');}function showImportModal(){showOverlay();const modal=$('import-modal');if(!modal)return;const textarea=$('data-import-text');if(textarea){textarea.value='';}modal.classList.remove('hidden');}let state=loadState()||{user:null,transactions:[],budgets:{},settings:{lowThreshold:20000,currency:'COP'},meta:{lastUpdated:nowISO(),version:'v8'}};const DEFAULT_CATEGORIES=['Alquiler','Cocina','Hogar','Cuotas','facturas','4thiago','Transporte','Skincare','Salud','Entretenimiento','Comida','Pet','Efectivo','Otros'];const el={greeting:$('greeting'),balanceNu:$('balance-nu'),balanceNequi:$('balance-nequi'),balanceBancolombia:$('balance-bancolombia'),balanceCash:$('balance-cash'),balanceTotal:$('balance-total'),balanceStatus:$('balance-status'),txForm:$('tx-form'),txDate:$('tx-date'),txType:$('tx-type'),incomeSourceRow:$('income-source-row'),expenseCategoryRow:$('expense-category-row'),transferFromRow:$('transfer-from-row'),cashConversionRow:$('cash-conversion-row'),cashConversionDetailsRow:$('cash-conversion-details-row'),incomeSource:$('income-source'),expenseCategory:$('expense-category'),transferFrom:$('transfer-from'),cashConversionType:$('cash-conversion-type'),cashConversionDetails:$('cash-conversion-details'),txAmount:$('tx-amount'),txAccount:$('tx-account'),txAccountRow:$('tx-account-row'),depositToNu:$('deposit-to-nu'),nuSplitRow:$('nu-split-row'),nuSplitAmount:$('nu-split-amount'),lastTxList:$('last-tx-list'),btnViewAll:$('btn-view-all'),btnViewAll2:$('btn-view-all-2'),alerts:$('alerts'),totalIncomes:$('total-incomes'),totalExpenses:$('total-expenses'),suggestedSavings:$('suggested-savings'),totalTransactions:$('total-transactions'),budgetsList:$('budgets-list'),btnSettings:$('btn-settings'),modalOverlay:$('modal-overlay'),setupModal:$('setup-modal'),viewAllModal:$('view-all-modal'),settingsModal:$('settings-modal'),budgetsModal:$('budgets-modal'),refreshBalances:$('refresh-balances'),btnExpensesReport:$('btn-expenses-report'),expensesReportModal:$('expenses-report-modal'),periodBreakdown:$('period-breakdown')};function getCategories(){const fromTx=(state.transactions||[]).filter(t=>t.type==='expense'&&t.category).map(t=>String(t.category).trim());const fromBudgets=Object.keys(state.budgets||{});const all=[...DEFAULT_CATEGORIES,...fromTx,...fromBudgets];const seen=new Set();const res=[];all.forEach(c=>{if(c&&!seen.has(c)){seen.add(c);res.push(c);}});return res;}function populateCategorySelects(){const cats=getCategories();const expSel=$('expense-category');if(expSel){const prev=expSel.value;expSel.innerHTML='';cats.forEach(c=>{const opt=document.createElement('option');opt.value=c;opt.textContent=c;expSel.appendChild(opt);});if(cats.includes(prev))expSel.value=prev;}const budgetSel=$('new-budget-name');if(budgetSel){const prev=budgetSel.value;budgetSel.innerHTML='<option value="" disabled selected>Seleccionar categoría</option>';cats.forEach(c=>{const opt=document.createElement('option');opt.value=c;opt.textContent=c;budgetSel.appendChild(opt);});if(cats.includes(prev))budgetSel.value=prev;}}function calcTotals(){let incomes=0,expenses=0,transfers=0,conversions=0;(state.transactions||[]).forEach(t=>{if(t.type==='income')incomes+=Number(t.amount);else if(t.type==='expense')expenses+=Number(t.amount);else if(t.type==='transfer')transfers+=Number(t.amount);else if(t.type==='cash-conversion')conversions+=Number(t.amount);});return {incomes,expenses,transfers,conversions};}function calcExpensesByCategory(){const map={};(state.transactions||[]).forEach(t=>{if(t.type==='expense'){const c=t.category||'Otros';map[c]=(map[c]||0)+Number(t.amount);}});return map;}function addTransaction(tx){const now=new Date();tx.timestamp=now.getTime();tx.hour=now.getHours();tx.minute=now.getMinutes();if(!tx.date)tx.date=now.toISOString().split('T')[0];state.transactions.push(tx);if(saveState(state))showToast('Transacción registrada correctamente','success');populateCategorySelects();renderAll();}function removeTransactionById(id){const idx=state.transactions.findIndex(t=>t.id===id);if(idx>=0){state.transactions.splice(idx,1);if(saveState(state))showToast('Transacción eliminada','success');populateCategorySelects();renderAll();}}function computeBalances(){let nu=state.user?Number(state.user.nu||0):0;let nequi=state.user?Number(state.user.nequi||0):0;let bancolombia=state.user?Number(state.user.bancolombia||0):0;let cash=state.user?Number(state.user.cash||0):0;const txs=(state.transactions||[]).slice().sort((a,b)=>(a.timestamp||0)-(b.timestamp||0));txs.forEach(tx=>{if(tx.type==='income'){if(tx.nuAllocated&&tx.nuAllocated>0){nu+=Number(tx.nuAllocated);const rest=Number(tx.amount)-Number(tx.nuAllocated);if(rest>0){if(tx.account==='nequi')nequi+=rest;else if(tx.account==='bancolombia')bancolombia+=rest;else if(tx.account==='cash')cash+=rest;}}else{if(tx.account==='nu')nu+=Number(tx.amount);else if(tx.account==='nequi')nequi+=Number(tx.amount);else if(tx.account==='bancolombia')bancolombia+=Number(tx.amount);else if(tx.account==='cash')cash+=Number(tx.amount);}}else if(tx.type==='expense'){if(tx.account==='nu')nu-=Number(tx.amount);else if(tx.account==='nequi')nequi-=Number(tx.amount);else if(tx.account==='bancolombia')bancolombia-=Number(tx.amount);else if(tx.account==='cash')cash-=Number(tx.amount);}else if(tx.type==='transfer'){const amount=Number(tx.amount);if(tx.from==='nu'&&tx.to==='nequi'){nu-=amount;nequi+=amount;}else if(tx.from==='nequi'&&tx.to==='nu'){nequi-=amount;nu+=amount;}else if(tx.from==='nu'&&tx.to==='bancolombia'){nu-=amount;bancolombia+=amount;}else if(tx.from==='bancolombia'&&tx.to==='nu'){bancolombia-=amount;nu+=amount;}else if(tx.from==='nequi'&&tx.to==='bancolombia'){nequi-=amount;bancolombia+=amount;}else if(tx.from==='bancolombia'&&tx.to==='nequi'){bancolombia-=amount;nequi+=amount;}else if(tx.from==='cash'&&tx.to==='nu'){cash-=amount;nu+=amount;}else if(tx.from==='cash'&&tx.to==='nequi'){cash-=amount;nequi+=amount;}else if(tx.from==='cash'&&tx.to==='bancolombia'){cash-=amount;bancolombia+=amount;}else if(tx.from==='nu'&&tx.to==='cash'){nu-=amount;cash+=amount;}else if(tx.from==='nequi'&&tx.to==='cash'){nequi-=amount;cash+=amount;}else if(tx.from==='bancolombia'&&tx.to==='cash'){bancolombia-=amount;cash+=amount;}}else if(tx.type==='cash-conversion'){const amount=Number(tx.amount);if(tx.conversionType==='to_cash'){if(tx.from==='nu'){nu-=amount;cash+=amount;}else if(tx.from==='nequi'){nequi-=amount;cash+=amount;}else if(tx.from==='bancolombia'){bancolombia-=amount;cash+=amount;}}else if(tx.conversionType==='from_cash'){if(tx.to==='nu'){cash-=amount;nu+=amount;}else if(tx.to==='nequi'){cash-=amount;nequi+=amount;}else if(tx.to==='bancolombia'){cash-=amount;bancolombia+=amount;}}}}});return {nu:Math.max(0,nu),nequi:Math.max(0,nequi),bancolombia:Math.max(0,bancolombia),cash:Math.max(0,cash),total:Math.max(0,nu+nequi+bancolombia+cash)};}function formatTime(date){return date.toLocaleTimeString('es-CO',{hour:'2-digit',minute:'2-digit',hour12:true});}function formatDateTime(timestamp){if(!timestamp)return'';const date=new Date(timestamp);return `${date.toLocaleDateString('es-CO')} ${formatTime(date)}`;}function getExpensesByPeriod(){const map={month:{},biweekly:{}};(state.transactions||[]).forEach(t=>{if(t.type==='expense'&&t.timestamp){const date=new Date(t.timestamp);const monthKey=date.toLocaleString('es-CO',{year:'numeric',month:'long'});const day=date.getDate();const biweeklyPeriod=day<=15?'Q1':'Q2';const biweeklyKey=`${monthKey} ${biweeklyPeriod}`;map.month[monthKey]=(map.month[monthKey]||0)+Number(t.amount);map.biweekly[biweeklyKey]=(map.biweekly[biweeklyKey]||0)+Number(t.amount);}});return map;}function renderAll(){if(!state.user){showSetup();populateCategorySelects();return;}hideAllModals();if(el.greeting)el.greeting.textContent=`Hola, ${state.user.name}`;const balances=computeBalances(),currency=state.settings.currency||'COP';if(el.balanceNu)el.balanceNu.textContent=formatCurrency(balances.nu,currency);if(el.balanceNequi)el.balanceNequi.textContent=formatCurrency(balances.nequi,currency);if(el.balanceBancolombia)el.balanceBancolombia.textContent=formatCurrency(balances.bancolombia,currency);if(el.balanceCash)el.balanceCash.textContent=formatCurrency(balances.cash,currency);if(el.balanceTotal)el.balanceTotal.textContent=formatCurrency(balances.total,currency);const low=Number(state.settings.lowThreshold||0);if(el.balanceStatus){el.balanceStatus.textContent=balances.total<low?'Saldo bajo':'Estable';el.balanceStatus.style.color=balances.total<low?'#ef4444':'#10b981';}if(el.lastTxList){el.lastTxList.innerHTML='';const sorted=(state.transactions||[]).slice().sort((a,b)=>(b.timestamp||0)-(a.timestamp||0));const last5=sorted.slice(0,5);if(last5.length===0){const li=document.createElement('li');li.className='tx-item';li.innerHTML='<div class="meta">No hay transacciones recientes</div>';el.lastTxList.appendChild(li);}else{last5.forEach(tx=>{const li=document.createElement('li');let cssClass='tx-item';if(tx.type==='transfer'){cssClass+=' tx-transfer';}else if(tx.type==='cash-conversion'){cssClass+=' tx-cash-conversion';}li.className=cssClass;let description='';let icon='';let amountDisplay='';if(tx.type==='transfer'){const fromName=tx.from==='nu'?'Nu':tx.from==='nequi'?'Nequi':tx.from==='bancolombia'?'Bancolombia':'Efectivo';const toName=tx.to==='nu'?'Nu':tx.to==='nequi'?'Nequi':tx.to==='bancolombia'?'Bancolombia':'Efectivo';description=`${fromName} → ${toName}`;icon='🔄 ';amountDisplay=`↔ ${formatCurrency(tx.amount,currency)}`;}else if(tx.type==='cash-conversion'){if(tx.conversionType==='to_cash'){const fromName=tx.from==='nu'?'Nu':tx.from==='nequi'?'Nequi':'Bancolombia';description=`${fromName} → Efectivo`;icon='💵 ';}else{const toName=tx.to==='nu'?'Nu':tx.to==='nequi'?'Nequi':'Bancolombia';description=`Efectivo → ${toName}`;icon='🏦 ';}amountDisplay=`↔ ${formatCurrency(tx.amount,currency)}`;}else if(tx.type==='income'){description=tx.source||'Ingreso';icon='⬆️ ';amountDisplay=`+ ${formatCurrency(tx.amount,currency)}`;}else{description=tx.category||'Gasto';icon='⬇️ ';amountDisplay=`- ${formatCurrency(tx.amount,currency)}`;}let accountInfo='';if(tx.type==='income'||tx.type==='expense'){accountInfo=tx.account==='nu'?'Nu':tx.account==='nequi'?'Nequi':tx.account==='bancolombia'?'Bancolombia':'Efectivo';}else if(tx.type==='transfer'){accountInfo='Transferencia';}else if(tx.type==='cash-conversion'){accountInfo='Conversión';}const timeDisplay=tx.timestamp?formatDateTime(tx.timestamp):tx.date;li.innerHTML=`<div><div><strong>${icon}${amountDisplay}</strong> <span class="meta">| ${accountInfo} | ${timeDisplay}</span></div><div class="meta">${description}</div>${tx.description?`<div class="meta" style="font-size:11px;color:#666;">${tx.description}</div>`:''}</div><div class="actions"><button class="btn-ghost" data-id="${tx.id}" data-action="view">Ver</button><button class="delete" data-id="${tx.id}" data-action="del">Eliminar</button></div>`;el.lastTxList.appendChild(li);});}}{const totals=calcTotals();if(el.totalIncomes)el.totalIncomes.textContent=formatCurrency(totals.incomes,currency);if(el.totalExpenses)el.totalExpenses.textContent=formatCurrency(totals.expenses,currency);if(el.totalTransactions)el.totalTransactions.textContent=(state.transactions||[]).length;const rec=suggestSavings(totals);if(el.suggestedSavings)el.suggestedSavings.textContent=rec.text;}renderAlerts(balances,calcTotals());renderBudgets(balances,calcTotals());renderExpensesPie();renderPeriodBreakdown();populateCategorySelects();state.meta.lastUpdated=nowISO();saveState(state);}function renderAlerts(balances,totals){if(!el.alerts)return;el.alerts.innerHTML='';if(balances.cash<10000&&balances.cash>0){const d=document.createElement('div');d.className='alert warning';d.textContent=`⚠️ Poco efectivo: ${formatCurrency(balances.cash,state.settings.currency)}. Considera hacer un retiro.`;el.alerts.appendChild(d);}if(balances.total<Number(state.settings.lowThreshold||0)){const d=document.createElement('div');d.className='alert danger';d.textContent=`Alerta: tu saldo total es bajo (${formatCurrency(balances.total,state.settings.currency)}). Revisa tu presupuesto.`;el.alerts.appendChild(d);}else{const d=document.createElement('div');d.className='alert good';d.textContent=`Saldo OK. Total disponible ${formatCurrency(balances.total,state.settings.currency)}.`;el.alerts.appendChild(d);}if(totals.expenses>totals.incomes){const d=document.createElement('div');d.className='alert danger';d.textContent=`Estás gastando más de lo que ingresas (Gastos ${formatCurrency(totals.expenses,state.settings.currency)} > Ingresos ${formatCurrency(totals.incomes,state.settings.currency)}).`;el.alerts.appendChild(d);}else{const ratio=totals.incomes>0?(totals.expenses/totals.incomes):0;if(ratio>0.8){const d=document.createElement('div');d.className='alert info';d.textContent=`Atención: tus gastos están en ${Math.round(ratio*100)}% de tus ingresos.`;el.alerts.appendChild(d);}}const spentByCat=calcExpensesByCategory();Object.keys(state.budgets).forEach(cat=>{const spent=spentByCat[cat]||0,budget=state.budgets[cat]||0;if(budget>0&&spent>budget){const d=document.createElement('div');d.className='alert danger';d.textContent=`Has excedido el presupuesto en ${cat}: gastado ${formatCurrency(spent,state.settings.currency)} / presupuesto ${formatCurrency(budget,state.settings.currency)}.`;el.alerts.appendChild(d);}});}function renderBudgets(){if(!el.budgetsList)return;el.budgetsList.innerHTML='';const spentByCat=calcExpensesByCategory();const keys=Object.keys(state.budgets);if(keys.length===0){el.budgetsList.innerHTML='<div class="meta">No hay presupuestos. Crea uno desde "Editar / Crear presupuestos".</div>';return;}keys.forEach(cat=>{const budget=Number(state.budgets[cat]||0),spent=Number(spentByCat[cat]||0);const percent=budget>0?Math.min(100,Math.round((spent/budget)*100)):0;const div=document.createElement('div');div.innerHTML=`<div style="display:flex;justify-content:space-between"><div>${cat}</div><div class="meta">${formatCurrency(spent,state.settings.currency)} / ${formatCurrency(budget,state.settings.currency)}</div></div><div class="progress"><i style="width:${percent}%;"></i></div>`;el.budgetsList.appendChild(div);});}function renderExpensesPie(){const canvas=$('expenses-pie');if(!canvas)return;const ctx=canvas.getContext('2d');const rect=canvas.getBoundingClientRect();canvas.width=Math.max(220,Math.floor(rect.width));canvas.height=160;ctx.clearRect(0,0,canvas.width,canvas.height);const data=calcExpensesByCategory();const entries=Object.entries(data).filter(e=>e[1]>0);if(entries.length===0){ctx.fillStyle='rgba(15,9,55,0.04)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#6b7280';ctx.font='12px Inter';ctx.textAlign='center';ctx.fillText('Sin gastos registrados',canvas.width/2,80);return;}const total=entries.reduce((s,e)=>s+e[1],0);let start=-Math.PI/2;const colors=['#7c3aed','#a78bfa','#c084fc','#f472b6','#d946ef','#c026d3','#8b5cf6','#f59e0b','#10b981'];entries.forEach((e,i)=>{const slice=e[1]/total*(Math.PI*2);ctx.beginPath();ctx.moveTo(canvas.width*0.33,80);ctx.arc(canvas.width*0.33,80,60,start,start+slice);ctx.closePath();ctx.fillStyle=colors[i%colors.length];ctx.fill();start+=slice;});ctx.font='12px Inter';ctx.textAlign='left';let y=12;entries.forEach((e,i)=>{ctx.fillStyle=colors[i%colors.length];ctx.fillRect(canvas.width*0.66,y,10,10);ctx.fillStyle='#374151';const percent=Math.round((e[1]/total)*100);ctx.fillText(`${e[0]} (${percent}%)`,canvas.width*0.66+16,y+10);y+=18;});}function renderPeriodBreakdown(){if(!el.periodBreakdown)return;el.periodBreakdown.innerHTML='';const periods=getExpensesByPeriod();const monthEntries=Object.entries(periods.month).sort((a,b)=>b[1]-a[1]);const biweeklyEntries=Object.entries(periods.biweekly).sort((a,b)=>b[1]-a[1]);if(monthEntries.length===0){el.periodBreakdown.innerHTML='<div class="meta">Sin datos</div>';return;}const monthDiv=document.createElement('div');monthDiv.innerHTML='<h4 style="font-size:13px;margin:0 0 8px 0;color:#666;">Por mes</h4>';monthEntries.forEach(([period,amount])=>{const pDiv=document.createElement('div');pDiv.style.display='flex';pDiv.style.justifyContent='space-between';pDiv.style.fontSize='13px';pDiv.style.padding='6px 0';pDiv.style.borderBottom='1px solid rgba(0,0,0,0.05)';pDiv.innerHTML=`<span>${period}</span><strong>${formatCurrency(amount,state.settings.currency)}</strong>`;monthDiv.appendChild(pDiv);});el.periodBreakdown.appendChild(monthDiv);}function showExpensesReport(){showOverlay();const modal=$('expenses-report-modal');if(!modal)return;modal.classList.remove('hidden');const container=$('expenses-report-container');if(!container)return;container.innerHTML='';const entries=Object.entries(calcExpensesByCategory()).filter(e=>e[1]>0);if(entries.length===0){container.innerHTML='<div class="meta">No hay gastos registrados.</div>';return;}entries.sort((a,b)=>b[1]-a[1]);const total=entries.reduce((s,e)=>s+e[1],0);entries.forEach(([cat,amt])=>{const percent=total>0?(amt/total)*100:0;const div=document.createElement('div');div.className='tx-row';div.innerHTML=`<div style="font-size:14px;color:var(--text)">${cat}</div><div style="font-weight:700">${Math.round(percent)}% &nbsp;&nbsp; ${formatCurrency(amt,state.settings.currency)}</div>`;container.appendChild(div);});const footer=document.createElement('div');footer.style.marginTop='8px';footer.className='meta';footer.textContent=`Total gastado: ${formatCurrency(total,state.settings.currency)}`;container.appendChild(footer);}function suggestSavings(totals){if(totals.incomes<=0)return {text:'Registra tus ingresos para recomendaciones.'};const recentSalary=state.transactions.find(t=>t.type==='income'&&String(t.source)==='Salario');const ratio=totals.incomes>0?(totals.expenses/totals.incomes):0;if(ratio>0.9)return {text:'Muy alto gasto. Reduce gastos inmediatos (≥10%).'};if(recentSalary){let recommendedPercent=20;if(ratio<0.4)recommendedPercent=30;else if(ratio<0.6)recommendedPercent=25;const savingsAmount=totals.incomes*(recommendedPercent/100);return {text:`${recommendedPercent}% de tus ingresos (${formatCurrency(savingsAmount,state.settings.currency)}) como ahorro.`};}return {text:'Considera ahorrar 15-20% de tus ingresos.'};}function updateFormVisibility(){const type=el.txType.value;const isIncome=type==='income';const isExpense=type==='expense';const isTransfer=type==='transfer';const isCashConversion=type==='cash-conversion';if(el.incomeSourceRow)el.incomeSourceRow.style.display=isIncome?'block':'none';if(el.expenseCategoryRow)el.expenseCategoryRow.style.display=isExpense?'block':'none';if(el.transferFromRow)el.transferFromRow.style.display=isTransfer?'block':'none';if(el.cashConversionRow)el.cashConversionRow.style.display=isCashConversion?'block':'none';if(el.cashConversionDetailsRow)el.cashConversionDetailsRow.style.display=isCashConversion?'block':'none';if(el.txAccountRow){el.txAccountRow.style.display=(isIncome||isExpense)?'block':'none';}if(el.depositToNu&&el.depositToNu.parentElement){el.depositToNu.parentElement.style.display=isIncome?'block':'none';}if(el.nuSplitRow){el.nuSplitRow.style.display=(isIncome&&el.depositToNu&&el.depositToNu.checked)?'block':'none';}if(el.txAccount){const currentValue=el.txAccount.value;el.txAccount.innerHTML='';if(isIncome||isExpense){el.txAccount.innerHTML=`<option value="nu">Nu</option><option value="nequi">Nequi</option><option value="bancolombia">Bancolombia</option><option value="cash">Efectivo</option>`;}if(currentValue&&Array.from(el.txAccount.options).some(opt=>opt.value===currentValue)){el.txAccount.value=currentValue;}}if(el.cashConversionDetails){const conversionType=el.cashConversionType?el.cashConversionType.value:'to_cash';el.cashConversionDetails.innerHTML='';if(conversionType==='to_cash'){el.cashConversionDetails.innerHTML=`<option value="nu_to_cash">Nu → Efectivo</option><option value="nequi_to_cash">Nequi → Efectivo</option><option value="bancolombia_to_cash">Bancolombia → Efectivo</option>`;}else{el.cashConversionDetails.innerHTML=`<option value="cash_to_nu">Efectivo → Nu</option><option value="cash_to_nequi">Efectivo → Nequi</option><option value="cash_to_bancolombia">Efectivo → Bancolombia</option>`;}}}function showOverlay(){if(el.modalOverlay)el.modalOverlay.classList.remove('hidden');}function hideOverlay(){if(el.modalOverlay)el.modalOverlay.classList.add('hidden');}function hideAllModals(){[$('setup-modal'),$('view-all-modal'),$('settings-modal'),$('budgets-modal'),$('export-modal'),$('expenses-report-modal'),$('portability-modal'),$('import-modal')].forEach(m=>{if(m)m.classList.add('hidden');});hideOverlay();}function showSetup(){showOverlay();if(el.setupModal)el.setupModal.classList.remove('hidden');if($('user-nu'))$('user-nu').value=state.user?state.user.nu.toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2}):'0,00';if($('user-nequi'))$('user-nequi').value=state.user?state.user.nequi.toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2}):'0,00';if($('user-bancolombia'))$('user-bancolombia').value=state.user?(state.user.bancolombia||0).toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2}):'0,00';if($('user-cash'))$('user-cash').value=state.user?state.user.cash.toLocaleString('es-CO',{minimumFractionDigits:2,maximumFractionDigits:2}):'0,00';}function showViewAll(){showOverlay();if(el.viewAllModal)el.viewAllModal.classList.remove('hidden');const container=$('all-tx-container');if(!container)return;container.innerHTML='';const typeFilter=$('tx-filter-type')?$('tx-filter-type').value:'all';const accountFilter=$('tx-filter-account')?$('tx-filter-account').value:'all';const searchFilter=$('tx-search')?$('tx-search').value:'';const filtered=filterTransactions(typeFilter,accountFilter,searchFilter).sort((a,b)=>(b.timestamp||0)-(a.timestamp||0));if(filtered.length===0){container.innerHTML='<div class="meta">No hay transacciones que coincidan con los filtros.</div>';return;}filtered.forEach(tx=>{const div=document.createElement('div');div.className=`tx-row ${tx.type==='transfer'?'tx-transfer':tx.type==='cash-conversion'?'tx-cash-conversion':''}`;let description='';let icon='';let amountDisplay='';if(tx.type==='transfer'){const fromName=tx.from==='nu'?'Nu':tx.from==='nequi'?'Nequi':tx.from==='bancolombia'?'Bancolombia':'Efectivo';const toName=tx.to==='nu'?'Nu':tx.to==='nequi'?'Nequi':tx.to==='bancolombia'?'Bancolombia':'Efectivo';description=`${fromName} → ${toName}`;icon='🔄 ';amountDisplay=`↔ ${formatCurrency(tx.amount,state.settings.currency)}`;}else if(tx.type==='cash-conversion'){if(tx.conversionType==='to_cash'){const fromName=tx.from==='nu'?'Nu':tx.from==='nequi'?'Nequi':'Bancolombia';description=`${fromName} → Efectivo`;icon='💵 ';}else{const toName=tx.to==='nu'?'Nu':tx.to==='nequi'?'Nequi':'Bancolombia';description=`Efectivo → ${toName}`;icon='🏦 ';}amountDisplay=`↔ ${formatCurrency(tx.amount,state.settings.currency)}`;}else if(tx.type==='income'){description=tx.source||'Ingreso';icon='⬆️ ';amountDisplay=`+ ${formatCurrency(tx.amount,state.settings.currency)}`;}else{description=tx.category||'Gasto';icon='⬇️ ';amountDisplay=`- ${formatCurrency(tx.amount,state.settings.currency)}`;}let accountInfo='';if(tx.type==='income'||tx.type==='expense'){accountInfo=tx.account==='nu'?'Nu':tx.account==='nequi'?'Nequi':tx.account==='bancolombia'?'Bancolombia':'Efectivo';}else if(tx.type==='transfer'){accountInfo='Transferencia';}else if(tx.type==='cash-conversion'){accountInfo='Conversión';}const timeDisplay=tx.timestamp?formatDateTime(tx.timestamp):tx.date;div.innerHTML=`<div><div><strong>${icon}${amountDisplay}</strong> <span class="meta">| ${accountInfo} | ${timeDisplay}</span></div><div class="meta">${description}</div>${tx.description?`<div class="meta" style="font-size:11px;color:#666;">${tx.description}</div>`:''}</div><div style="display:flex;gap:6px;align-items:center"><button class="btn-ghost" data-action="revert" data-id="${tx.id}">Eliminar</button></div>`;container.appendChild(div);});}function showSettings(){showOverlay();if(el.settingsModal)el.settingsModal.classList.remove('hidden');if($('settings-low-threshold'))$('settings-low-threshold').value=formatCurrency(state.settings.lowThreshold||20000,state.settings.currency).replace('$','').trim();if($('settings-currency'))$('settings-currency').value=state.settings.currency||'COP';}function showBudgets(){showOverlay();if(!el.budgetsModal)return;el.budgetsModal.classList.remove('hidden');const list=$('budgets-form-list');if(!list)return;list.innerHTML='';const keys=Object.keys(state.budgets);const cats=getCategories();if(keys.length===0){const p=document.createElement('div');p.className='meta';p.textContent='Aún no hay presupuestos. Agrega uno abajo.';list.appendChild(p);}let i=0;keys.forEach(k=>{const div=document.createElement('div');div.className='row';div.style.display
